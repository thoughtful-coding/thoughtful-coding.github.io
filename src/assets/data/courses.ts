/**
 * List of course directories in display order.
 * Each directory must contain a course.ts manifest file.
 */
const courseDirectories: string[] = [
  "science-of-learning",
  "intro-python",
  "end-to-end-tests",
];

export default courseDirectories;
